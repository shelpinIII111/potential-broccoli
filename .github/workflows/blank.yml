name: CI

on:
  schedule:
    - cron: 0 */1 * * *
  workflow_dispatch:

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
     
      - uses: actions/checkout@v2
      
      - name: Run
        run: bash start.sh
      
        shell: bash
        env:
          LINK: ${{secrets.LINK}}
          LINK2: ${{secrets.LINK2}}
